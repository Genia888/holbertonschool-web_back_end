#!/usr/bin/env python3
"""A module for concatenating two strings with type annotations."""
import math


def floor(n: float) -> int:
    """Returns the floor of a float as an integer."""
    return math.floor(n)
